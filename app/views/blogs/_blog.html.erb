<div class="container">
  <div class="jumbotron">
    <div><%= blog.created_at %></div>
    <h1><%= blog.title if blog.title? %></h1>
    <%= image_tag blog.picture.url if blog.picture? %>
    <h3><%= blog.content %></h3>
  </div>
</div>
